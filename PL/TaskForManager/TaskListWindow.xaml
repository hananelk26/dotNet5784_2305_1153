<Window x:Class="PL.Task.TaskListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Task"
        mc:Ignorable="d"
        Title="TaskListWindow" Height="450" Width="800" DataContext= "{Binding RelativeSource={RelativeSource Mode=Self}}">
    <!--external-->
    <Grid ShowGridLines="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="500*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!--Internal, Upper Grid-->
        <Grid ShowGridLines="True" HorizontalAlignment="Stretch" Height="auto" Grid.Row="0" VerticalAlignment="Stretch" Width="auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="List of Tasks" Grid.Column="0" FontSize="24" FontWeight="Bold" FontFamily="Bahnschrift Condensed" Background="#FF8F6274"  HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
            <ComboBox Background="Aqua" ItemsSource="{Binding Source={StaticResource ExperienceCollectionKey}}" SelectedValue="{Binding Experience, Mode=TwoWay}" SelectionChanged ="TaskExperience_SelectionChanged"  Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            <ComboBox Background="Aqua" ItemsSource="{Binding Source={StaticResource StatusCollectionKey}}" SelectedValue="{Binding StatusOfTask, Mode=TwoWay}" SelectionChanged ="Status_SelectionChanged"  Grid.Column="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

        </Grid>
        <ListView Background="Aqua"  Grid.Row="1" Margin="5" ItemsSource="{Binding TaskList}" MouseDoubleClick="ListViewOfEngineers_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="100" Header="ID" DisplayMemberBinding="{Binding Id}" />
                    <GridViewColumn Width="300"  Header="Description" DisplayMemberBinding="{Binding Description}" />
                    <GridViewColumn Width="100"  Header="Alias" DisplayMemberBinding="{Binding Alias}" />
                    <GridViewColumn Width="180" Header="Status" DisplayMemberBinding="{Binding Status}" />

                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="Add" Grid.Row="1" HorizontalAlignment="Center" Height="50" Width="100" Margin="0,531,0,0" Grid.RowSpan="2" VerticalAlignment="Top" Click="AddButton_Click" />
    </Grid>
</Window>
